const numDiv = function(number){
  const arrayHalves = [];
  let halve = number / 2;
  arrayHalves.push(halve);
  arrayHalves.push(halve);
  console.log(arrayHalves);  
}

numDiv(4);
numDiv(10);
numDiv(5);